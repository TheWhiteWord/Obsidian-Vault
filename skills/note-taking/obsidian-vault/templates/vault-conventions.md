# <Vault name> — writing conventions

> Mutable, per-vault conventions file. It grows through interaction — when a
> session reveals a rule worth keeping, it lands here. Keep each section
> tight; the file is loaded on demand, not every turn.

## Rules

<!-- Hard rules for this vault. Example:
- Never omit the schema's required frontmatter fields.
- Never edit generated files (`<!-- generated -->`).
-->

## Style & structure

<!-- Defaults of style and structure that make notes uniform. Example:
- Titles are sentence case.
- Every new note links back to its folder INDEX.
- Callouts only for warnings and tips.
-->

## Domain notes

<!-- What is specific to this vault's domain. Example:
- Philosophy notes carry a `thinker` tag plus the period.
- Research notes cite sources in a `sources` section.
-->

## Change log

<!-- When this file is edited and why (propose → confirm → record). -->
